# Per-Model Timeout

- **id**: per-model-timeout
- **status**: implemented
- **created**: 2026-07-31
- **updated**: 2026-07-31
- **summary**: Add per-model timeouts (60s) so no single slow model exhausts the chain budget, and make the aggregate timeout configurable (default: unlimited) so users always get a result.
