{
  "name": "review-action",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp -r src/prompts dist/prompts",
    "test": "node --test dist/**/*.test.js",
    "package": "ncc build src/index.ts -o dist && cp -r src/prompts dist/prompts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.6.0"
  }
}
